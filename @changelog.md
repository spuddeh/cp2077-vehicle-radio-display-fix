# Changelog

## [Unreleased] - 0.1.0

### Added

- The vehicle radio display is written from the receiver after the Radioport handoff, after the
  vehicle's mount handler, and 0.3 s later: `VehRadioState`, `VehRadioStationName` and the
  component's `m_radioState`.
